name: PR Check

on:
  pull_request:
    branches:
      - main

jobs:
  pr-test:
    runs-on: ubuntu-latest

    steps:
      - name: Print branch
        run: |
          echo "PR check running for branch: ${{ github.head_ref }}"
